/** @type {import('@remix-run/dev').AppConfig} */
export default {
	ignoredRouteFiles: ['**/.*'],
	// appDirectory: "app",
	// assetsBuildDirectory: "public/build",
	// publicPath: "/build/",
	// serverBuildPath: "build/index.js",
	// routes: "app/routes",
	// change route _index to app
	// routes(defineRoutes) {
	// 	return defineRoutes(route => {
	// 		route('/', 'routes/app.tsx', { index: true }, () => {
	// 			route('sign-in', 'routes/sign-in.tsx');
	// 		});
	// 	});
	// },
};
